# ── FastAPI Docker 경량 의존성 ──────────────────────────────────────────────
# torch는 Dockerfile에서 CPU-only로 별도 설치하므로 여기 포함하지 않음

# --- Core Web ---
fastapi==0.136.0
uvicorn==0.45.0
httpx==0.28.1
python-dotenv==1.2.2
python-multipart==0.0.27
pydantic==2.13.3
pydantic-settings==2.14.0
starlette==1.0.0

# --- Data / ML ---
pandas==2.3.3
numpy==2.2.6
networkx==3.4.2
scikit-learn==1.7.2
# models/ensemble_ranker.pkl 안에 LGBMRanker(objective=lambdarank)가 들어 있다.
# 이 패키지가 없으면 pickle.load 가 ModuleNotFoundError 를 내고, ensemble_client
# 가 그것을 삼켜 rank_pois 가 candidates[:top_k] 로 떨어진다. 즉 추천 순위가
# 합쳐진 순서 그대로 나간다. 실제로 그 상태로 운영됐다(#217).
lightgbm==4.7.0
scipy==1.15.3
sentence-transformers==5.4.1
transformers==5.7.0
tokenizers>=0.21.0
safetensors==0.7.0

# --- External DB / API ---
chromadb==1.5.8
groq==1.2.0
supabase==2.30.0
psycopg2-binary==2.9.12

# --- Celery ---
celery[redis]>=5.3,<6
flower>=2.0,<3

# --- Media worker deps는 Dockerfile.runpod / Dockerfile.worker에서 별도 설치 ---
# gTTS, soundfile, cloudinary는 media worker 전용 (click 충돌 방지)
cloudinary>=1.41,<2

# --- Utilities ---
orjson==3.11.8
requests==2.33.1
tqdm==4.67.3
joblib==1.5.3
PyYAML==6.0.3
pillow==12.2.0
regex==2026.4.4
