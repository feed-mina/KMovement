# Install PyTorch separately for the target CUDA runtime before this file.
# Example on a compatible GPU image:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

diffusers>=0.31.0,<1
transformers>=4.46.0,<5
accelerate>=0.33.0,<1
safetensors>=0.4.5,<1
pillow>=10,<12
imageio-ffmpeg>=0.5,<1
